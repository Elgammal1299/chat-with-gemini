import 'package:flutter/material.dart';

class AppColors {
  static Color primaryColor = Color(0xFF4CAF50); // Primary color for the app
  static Color userMessageColor = Color(0xFFDCF8C6); // رسالة المستخدم
  static Color otherMessageColor = Colors.white; // رسالة الطرف الآخر
  static Color sendButtonColor = Color(0xFF4CAF50); // زر الإرسال
  static Color chatBackground = Color(0xFFF9F9FB); // خلفية الشاشة
  static Color inputBackground = Colors.white; // خلفية مربع الإدخال
  static Color messageTextColor = Colors.black87; // لون النص
  static Color black = Colors.black; // لون النص
  static Color timeTextColor = Colors.grey; // وقت الرسالة
  static Color blue = Colors.blueAccent;
}
